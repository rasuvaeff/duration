# Examples

Runnable scripts demonstrating `rasuvaeff/duration`.

Install dependencies first, then run a script with PHP:

```bash
composer install
php examples/basic.php
```

| Script | Shows | Needs server? |
|---|---|---|
| `basic.php` | Factories, conversions, arithmetic, comparison | no |
